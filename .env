# API URL for production (or development if you want to bypass proxy)
VITE_API_URL=
# API Base Path (default is /api)
VITE_API_BASE_PATH=/api
# Proxy target for local development (default is http://localhost:8080)
VITE_PROXY_TARGET=http://localhost:8080
